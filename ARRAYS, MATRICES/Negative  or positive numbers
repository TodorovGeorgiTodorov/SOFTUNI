function negativeOrPositive(arr) {

    let result = [];
    for (let i in arr ){
        if(arr[i] < 0 ){
            result.unshift(arr[i]);
        }else{
            result.push(arr[i]);
        }
    }
    console.log(result.join('\n'));
}
negativeOrPositive([-1, -2, 3, 0]);