function sumFirstLast(arr) {

    let num1 = +arr[0];
    let num2 = +arr[arr.length -1];
    function sum(num1, num2) {
        console.log( num1 + num2);
    }
    sum(num1, num2);
}
sumFirstLast(['5', '10']);