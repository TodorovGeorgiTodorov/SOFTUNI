function lastKNubersSequence(n, k) {

    let result = [1,1];
    for (let i = 0; i < n -1; i++) {

        for (let j = 0; j < k -1; j++) {
            let num =  result[result.length - j] + result[result.length - j -1];
            result.push(num);


        }
    }
}
lastKNubersSequence(6, 3);