function firstAndLastKNumbers(arr) {

    let k = arr.shift();
    let result = arr;

    let first = result.slice(0,k);
    let last = result.slice(result.length - k, k + 1);
    console.log(first);
    console.log(last);
}
firstAndLastKNumbers([2, 7, 8, 9]);